<h1>Rock 'n Rails!</h1>
<% @records.each do |record| %>
  <p>Title: <%= record.title %></p>
  <p>Artist: <%= record.artist %></p>
  <img src="<%= record.cover_art %>" alt="record cover image">

  <br>
  <%= link_to "Show page", record_path(record) %>
  <a href="/records/<%= record.id %>" data-method="delete">asdsad</a>
<% end %>
